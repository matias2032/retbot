package dev258.retbotbackend.utilizador.enums;

/**
 * Corresponde ao tipo ENUM 'plataforma_social' definido no schema PostgreSQL.
 * Mantido sincronizado manualmente com a migration correspondente
 * (a migration será criada na fase de Flyway).
 */
public enum PlataformaSocial {
    X,
    BLUESKY,
    MASTODON,
    LINKEDIN,
    THREADS
}
